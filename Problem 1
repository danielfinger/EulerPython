total = 0

# check each number between 1 and 999
# if it is a multiple of 3 or 5, it is added to the total
# numbers which are multiples of both, will only be added once

for x in range(1, 1000):
    if x%3 == 0:
        total = total + x
    elif x%5 == 0:
        total = total + x
        
print "total is %i" % (total)
